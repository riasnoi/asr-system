apiVersion: v1
kind: Secret
metadata:
  name: asr-vault-credentials
  namespace: asr-system
type: Opaque
# Do not commit real values. Create this secret via CLI:
#
#   kubectl -n asr-system create secret generic asr-vault-credentials \
#     --from-literal=VAULT_ADDR=https://vault.your-domain.com:8200 \
#     --from-literal=VAULT_ROLE_ID=<role-id> \
#     --from-literal=VAULT_SECRET_ID=<secret-id>
#
# This file exists as a schema reference only.
stringData: {}
