apiVersion: v1
kind: Service
metadata:
  name: asr-online
  namespace: asr-system
spec:
  selector:
    app: asr-online
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      nodePort: 30080
  type: NodePort
