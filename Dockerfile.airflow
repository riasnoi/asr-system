FROM apache/airflow:2.10.3

# The DAG uses only PythonOperator + the kubernetes Python client,
# which is already bundled with the base Airflow image.
# This file pins the exact version used in production.
